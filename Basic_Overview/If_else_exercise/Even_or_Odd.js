// If a number is divided to 2 and reminder is zero then this number is even

const number = 11;

if (number % 2 == 0) {
    console.log(`${number} is Even`);
} else {
    console.log(`${number} is Odd`);
}

// output ==> 11 is Odd